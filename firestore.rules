rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Reglas de DESARROLLO: cualquier usuario autenticado puede leer/escribir
    // TODO: Reemplazar por reglas restrictivas antes de producción
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
